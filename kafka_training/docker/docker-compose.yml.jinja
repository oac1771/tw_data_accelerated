services:
  broker:
    container_name: broker
    image: "kafka:latest"
    command: entrypoint.sh
    ports:
      - "9092:9092"
    networks:
      - kafka-net

networks:
  kafka-net:
    driver: bridge
